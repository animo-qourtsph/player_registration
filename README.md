# Player v33 — Live Payment Methods

Payment methods now come from Supabase through registration-api.

Only methods enabled in Admin are displayed. The Player sees the live account name, account number/payment ID, instructions, and QR image.
